{
  "name": "json-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "json-server": "^0.16.3",
    "query-string": "^7.0.0"
  },
  "devDependencies": {
    "faker": "^5.5.3",
    "nodemon": "^2.0.7"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon server.js",
    "start": "npm run generate-data && node server.js",
    "generate-data": "node generate-data.js"
  },
  "author": "",
  "license": "ISC"
}
